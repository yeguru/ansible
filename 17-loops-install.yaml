- name: to check loops
  hosts: local
  connection: local
  tasks: 
  - name: to print the items
    ansible.builtin.debug:
     msg: "Hello {{ item }}"
    loop:
    - Suresh
    - Ramesh
    - Mahesh
    - Prakash

